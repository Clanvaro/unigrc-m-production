# NPM Configuration for Anti-Regression Protection
# This ensures exact versions are installed and prevents unexpected updates

# Always save exact versions (no ^ or ~)
save-exact=true

# Enforce Node.js version from package.json engines field
engine-strict=true

# Use npm ci in production (reads from package-lock.json only)
# Run: npm ci --prefer-offline --no-audit
